import styles from "./MainContent.module.scss";

export const MainContent = () => {
  return (
    <main className={styles.main}>
      <h1 className={styles.title}>We are ready for your creativity!</h1>
    </main>
  );
};
