import styles from "./Footer.module.scss";

export const Footer = () => {
  return (
    <footer className={styles.footer}>
      <span className={styles.text}>May the force be with you...</span>
    </footer>
  );
};
